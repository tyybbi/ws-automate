---
  - hosts: all

    vars_prompt:
      - name: usernam
        prompt: "Username to use in config files"
        private: no
    vars:
      is_laptop: false

    gather_facts: true
    force_handlers: true
    tasks:
      - include: "install-packages.yaml"
      - include: "create-user.yaml"
      - include: "copy-configs.yaml"
      - include: "git-some-more.yaml"
      - include: "system-tweaks.yaml"

